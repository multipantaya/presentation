import 'package:flutter/material.dart';

class Gradients {
  final List<Color> primarylinearGradiant = const [
    Color(0xFF135A30),
    Color(0xFF38A700)
  ];
  final List<Color> transparentGradiant = const [
    Color(0x00FFFFFF),
    Color(0x00FFFFFF)
  ];
}